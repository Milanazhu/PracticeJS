function pow(n) {
   const a = n * n // Задаем переменную а 
    return a; // Возвращать будем а
}
pow(3);

const result = pow(3); // задаем переменную которая возвращает 3 * 3 функция
console.log(result); // Выводим в консоль 

