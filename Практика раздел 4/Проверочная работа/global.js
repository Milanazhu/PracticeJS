//Создайте функцию, которая бы прибавляла к глобальной переменной count + 1 и выводила результат в консоль.

let count = 2; // Глобальная переменная

function numCount() {
    count += 1; // Увеличиваем count на 1
    console.log(count); // Выводим результат в консоль
}

numCount(); 

